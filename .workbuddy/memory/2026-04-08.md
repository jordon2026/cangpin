# 2026-04-08 工作日志

## 安全漏洞修复 - museum-server 全部漏洞已修复

基于上午安全审计报告，下午完成了所有高危/中危漏洞的代码修复：

### 已修复清单

| # | 文件 | 修复内容 |
|---|------|---------|
| VUL-001 | SaTokenConfig.java | 注册 SecurityInterceptor 到 addInterceptors()，XSS防护真正生效 |
| VUL-002 | SecurityInterceptor.java | 重写：真正拦截XSS（返回400而非只打印），增加路径遍历防护；移除CSRF try-catch吞掉逻辑，改为注释说明Sa-Token Header方式天然防CSRF |
| VUL-003 | application.yml | 数据库/Redis密码改为 ${DB_PASSWORD} 等环境变量，不再硬编码；新增 .env.example 和 .gitignore |
| 越权 | 5个Controller | 所有接口添加 @SaCheckPermission("module:resource:action") 权限校验 |
| 日志明文密码 | OperLogAspect.java | 添加 desensitize() 方法，通过正则替换 password/token 等敏感字段值为 *** |
| 弱密码重置 | SysUserController.java | resetPwd 改为生成随机10位强密码（大小写+数字+特殊符号），不再硬编码 admin123 |
| CORS双重配置 | CorsConfig.java | 移除重复的 CorsFilter Bean，统一由 SaTokenConfig 管理；限制为 allowedOrigins 配置的域名 |
| Token永不过期 | application.yml | active-timeout 改为 7200（2小时无操作过期） |
| SQL日志泄露 | application.yml | auth mapper 日志级别改为 info |
| IP伪造 | SecurityInterceptor.java | 优先取 X-Real-IP（Nginx设置），避免直接信任可伪造的 X-Forwarded-For |


完成了对 museum-server 后端项目的完整安全审计，输出了 HTML 格式安全报告。

### 审计结论（综合风险评分 68/100 - 中高风险）

**高危（P0 必须修复）：**
1. XSS防护拦截器 SecurityInterceptor 已编写但未注册到 addInterceptors()，完全无效
2. CSRF Same-Token 校验被 try-catch 静默跳过（"开发模式"注释未删除），形同虚设
3. 数据库密码 `jordon` 明文硬编码在 application.yml（同时使用 root 账户）

**中危（P1 上线前修复）：**
4. 所有 Controller 缺失 @SaCheckPermission 注解，只校验登录态不校验权限（水平越权风险）
5. OperLogAspect 操作日志记录明文密码（UserAddDTO.password 字段无 @JSONField(serialize=false)）
6. 重置密码接口硬编码为 admin123 弱默认密码
7. CORS 允许所有来源 + allowCredentials=true，双重配置冲突
8. X-Forwarded-For 可伪造导致 IP 限流机制可被绕过

**低危（P2 建议修复）：**
9. Token active-timeout=-1 永不过期
10. Swagger /doc.html 生产环境可公开访问
11. Redis NON_FINAL 反序列化配置存在攻击面
12. StdOutImpl SQL 日志打印完整参数

**已正确实现的安全功能：**
- BCrypt 密码加密 ✅
- 登录失败 5 次锁定 30 分钟 ✅
- 图形验证码 + 一次性使用 ✅
- Sa-Token Token 认证 ✅
- MyBatis-Plus 参数化查询防 SQL 注入 ✅
- 全局异常处理（不暴露堆栈） ✅

报告路径：`c:\Users\41499\AppData\Roaming\WorkBuddy\...\security-audit-report.html`
