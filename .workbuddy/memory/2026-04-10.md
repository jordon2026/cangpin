# 2026-04-10 工作日志

## 模拟代码全面排查与修复

### 已完成修复

#### 1. Profile 个人信息页面
- **后端**：AuthService 添加 `updateUserInfo()` 方法
- **后端**：AuthController 添加 `PUT /api/v1/auth/profile` 接口
- **前端**：profile/index.vue 对接真实 API
  - 添加 `onMounted` 从后端获取用户信息
  - 修复 `handleSave` 调用真实 API

#### 2. Dashboard 仪表盘
- **后端**：创建 DashboardDTO 统计类
- **后端**：AuthService 添加 `getDashboardStats()` 统计方法
- **后端**：SysUserMapper 添加 `execSql()` 原生查询方法
- **后端**：AuthController 添加 `GET /api/v1/auth/dashboard` 接口
- **前端**：dashboard/index.vue 对接真实 API

#### 3. 藏品管理模块（完整）
- **后端**：Collection Entity
- **后端**：CollectionMapper
- **后端**：CollectionDTO
- **后端**：CollectionService (CRUD)
- **后端**：CollectionController
- **前端**：collection.js API 模块
- **前端**：collection/info/index.vue 对接真实 API

#### 4. 库房管理模块（完整）
- **后端**：Storage Entity
- **后端**：StorageMapper
- **后端**：StorageDTO
- **后端**：StorageService (CRUD)
- **后端**：StorageController
- **前端**：storage.js API 模块
- **前端**：storage/info/index.vue 对接真实 API

#### 5. 入库管理模块（完整）
- **后端**：Inbound Entity
- **后端**：InboundMapper
- **后端**：InboundDTO
- **后端**：InboundService (申请/审批)
- **后端**：InboundController
- **前端**：inbound.js API 模块
- **前端**：inout/inbound/index.vue 对接真实 API

#### 6. 出库管理模块（部分）
- **后端**：Outbound Entity
- **后端**：OutboundMapper
- **后端**：OutboundDTO
- **后端**：OutboundService (申请/审批/归还)
- **后端**：OutboundController
- **前端**：outbound.js API 模块

#### 7. 环境监测模块
- **前端**：environment/data/index.vue 移除模拟刷新，改为从库房列表加载

### 数据库表结构
- **新增**：db/collection_table.sql
  - collection (藏品表)
  - storage (库房表)
  - collection_category (藏品分类表)
  - inbound (入库记录表)
  - outbound (出库记录表)
  - rfid_tag (RFID标签表)
  - rfid_device (RFID设备表)
  - inventory_task (盘点任务表)
  - inventory_record (盘点记录表)
  - repair_order (修复工单表)
  - loan_apply (外借申请表)
  - environment_data (环境监测数据表)

### 待完成
- 执行 collection_table.sql 建表

### 注意事项
- 执行建表 SQL 前需先在数据库中创建表
- 藏品、出库等页面需等待建表后测试

---

## 2026-04-10 晚上 (继续)

### 完成模块

#### 8. RFID管理模块（完整）
- **后端**：RfidTag Entity, RfidTagMapper, RfidTagDTO, RfidTagService, RfidTagController
- **后端**：RfidDevice Entity, RfidDeviceMapper, RfidDeviceDTO, RfidDeviceService, RfidDeviceController
- **前端**：rfid/tag/index.vue 对接真实 API（移除模拟代码）

#### 9. 盘点管理模块（完整）
- **后端**：InventoryTask Entity, InventoryRecord Entity
- **后端**：InventoryTaskMapper, InventoryRecordMapper
- **后端**：InventoryTaskDTO, InventoryRecordDTO
- **后端**：InventoryTaskService, InventoryController
- **前端**：inventory/task/index.vue 对接真实 API（移除模拟代码）

#### 10. 修复管理模块（完整）
- **后端**：RepairOrder Entity, RepairOrderMapper, RepairOrderDTO
- **后端**：RepairOrderService, RepairController
- **前端**：repair/order/index.vue 对接真实 API（移除模拟代码）

#### 11. 外借管理模块（完整）
- **后端**：LoanApply Entity, LoanApplyMapper, LoanApplyDTO
- **后端**：LoanApplyService, LoanController
- **前端**：loan/apply/index.vue 对接真实 API（移除模拟代码）

### 后端新增文件清单
- Entity: RfidTag, RfidDevice, InventoryTask, InventoryRecord, RepairOrder, LoanApply (6个)
- Mapper: RfidTagMapper, RfidDeviceMapper, InventoryTaskMapper, InventoryRecordMapper, RepairOrderMapper, LoanApplyMapper (6个)
- DTO: RfidTagDTO, RfidDeviceDTO, InventoryTaskDTO, InventoryRecordDTO, RepairOrderDTO, LoanApplyDTO (6个)
- Service: RfidTagService, RfidDeviceService, InventoryTaskService, RepairOrderService, LoanApplyService (5个)
- Controller: RfidTagController, RfidDeviceController, InventoryController, RepairController, LoanController (5个)

### 前端修复页面
- rfid/tag/index.vue - RFID标签管理
- inventory/task/index.vue - 盘点任务管理
- repair/order/index.vue - 修复工单管理
- loan/apply/index.vue - 外借申请管理
