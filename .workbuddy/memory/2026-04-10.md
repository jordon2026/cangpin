# 2026-04-10 工作日志

## 宝塔部署改造

- 决策：改用宝塔面板 Nginx 替代 Docker nginx 容器
- 修改 `docker-compose.yml`：去掉 nginx service，只保留 mysql/redis/backend 三个容器
- 新增 `nginx.bt.conf`：宝塔专用 nginx 配置，proxy_pass 改为 `http://127.0.0.1:8080/`
- 新增 `DEPLOY-BAOTA.md`：完整宝塔部署操作指南（含 Docker Compose 启动 + 宝塔站点配置 + SSL + 常见问题）
